# React + TypeScript + Vite

## To run the frontend

1. cd into the 'prosjekt2' folder
2. run `npm install`
3. run `npm run dev`
4. open [http://localhost:5173/](http://localhost:5173/) in your browser
